<% blocks.forEach(function (block) { %>
## <%= block.name %> (<%= block.endpoint %>)

<%= block.summary %>

<%= block.body %>
### Inputs

||**Param**||**Type**||**Description**||**Required?**||
<% block.inputs.forEach(function (input) { %>||<%= input.name %>||<%= input.type %>||<%= input.description %>||<%= input.required ? 'true' : 'false' %>||
<% }); %>

### Responses

||*Code*||*Type*||*Description*||
<% block.responses.forEach(function (resp) { %>||<%= resp.code %>||<%= resp.type %>||<%= resp.description %>||
<% }); %>
<% }); %>
